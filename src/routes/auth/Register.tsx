import styled from "styled-components";

type RegisterProps = {

}

const Register = (props: RegisterProps) => {
    return (
        <div>
            <h1>Register</h1>
        </div>
    );
}

export default Register;